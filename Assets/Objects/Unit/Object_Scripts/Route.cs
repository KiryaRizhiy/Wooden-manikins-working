using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Route {

    private string scr = "Route";
    private float _MinDistance, _MaxDistance;
    private List<Vector3> Points = new List<Vector3>();

    public Route(Vector3 StartPoint, Vector3 EndPoint, float MinDistanceToTarget=0, float MaxDistanceToTarget = float.MaxValue)
    {

    }
}